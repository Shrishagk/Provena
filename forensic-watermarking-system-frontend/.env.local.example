# Local air-gapped gateway. For a LAN deployment, use its private HTTPS URL.
NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8080
# Recipient-local agent on the keyholder's device. Do not point this at the gateway.
NEXT_PUBLIC_RECIPIENT_AGENT_BASE_URL=http://127.0.0.1:8081
